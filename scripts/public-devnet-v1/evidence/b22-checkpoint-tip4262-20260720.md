# B-22 — Near-tip Path A checkpoint tip-4262 (2026-07-20)

## Why

Log max was **4173** while live tip reached **4262** (lag **89**). Tall-tip JOIN / B-50 pin needs a near-tip attestation for lane 3 SUMMARY.

## Actions

1. Hetzner: sourced offline `/root/.mfn/checkpoint-signer.env` (never committed).
2. `publish-checkpoint-log.sh --apply` as `permawrite-maintainer-path-a-2` at tip **4262**.
3. Public seed anchors merged; faucet idle and untouched (no mfnd restart).

## Verify

```
checkpoint_log_verify_ok entries=11
max_tip_height=4262
```

## Note

mfnd rebuild-roll still held for CI `#29725270815` GREEN (B-29+B-64 stack).
