# B-22 - Near-tip Path A checkpoint refresh tip-4159 (2026-07-20)

## Why

Live tip advanced to **4159** while log max was **4148** (lag 11). Tall-tip JOIN / B-50/B-54/B-57 pin needs a near-tip attestation.

## Actions

1. Hetzner: sourced offline `/root/.mfn/checkpoint-signer.env` (never committed).
2. `publish-checkpoint-log.sh --apply` as `permawrite-maintainer-path-a-2` at tip **4159**.
3. Manifest `seed_nodes` merged into `anchor_peers` (`5.161.201.73:19001-19003`).
4. Faucet left untouched (idle: busy=false, pending_jobs=0).

## Verify

```
checkpoint_log_verify_ok entries=8
max_tip_height=4159
```

## Note

Follows B-57/F68 Windows bootstrap fix (`268ceb7`). mfnd roll still held for CI+B-51.