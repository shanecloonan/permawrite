# B-22 — Near-tip Path A checkpoint refresh tip-4148 (2026-07-20)

## Why

Live tip advanced to **4148** while log max was **4133** (lag 15). Tall-tip JOIN / B-50/B-54 pin-then-fund needs a near-tip attestation.

## Actions

1. Hetzner: sourced offline `/root/.mfn/checkpoint-signer.env` (never committed).
2. `publish-checkpoint-log.sh --apply` as `permawrite-maintainer-path-a-2` at tip **4148**.
3. Manifest `seed_nodes` merged into `anchor_peers` (`5.161.201.73:19001-19003`).
4. Faucet left untouched (was idle at publish).

## Verify

```
checkpoint_log_verify_ok entries=7
max_tip_height=4148
```

## CI note (not this unit)

CI `#29715111633` on B-48 (`69df8fa`): ubuntu `test` **FAILED** —
`b3_legacy_challenge_rejected_when_enabled` (attempt 1) then retry hit
`public_devnet_hub_reaches_height_one_within_one_slot_duration` timeout.
Owner: lane 4 (+ lane 1 fix-forward). Lane 7 did not touch consensus/P2P Rust.