# B-77 — B-71 mfnd roll + tip-4400 Path A checkpoint (2026-07-20)

## Roll

- Prebuild binary already contained `is_persistable_peer_addr` (B-71).
- CI `#29739903305` GREEN on `62a9c02` (B-74); HEAD `03ec40c` was docs-only since that GREEN.
- `MFN_ROLL_ALLOW_RED_CI=1` used after manual verify (wave25 CI still in_progress would otherwise fail-closed).
- `vps-roll-mfnd.sh --apply --skip-build --skip-pull`: tip **4398 -> 4399** (then **4400+**); faucet untouched.
- `scrub-vps-peers-json` + `assert-vps-peers-clean`: OK (no ephemeral pollution).

## Checkpoint

- Path A `permawrite-maintainer-path-a-2` published tip **4400** (entries=13), anchors `:19001-19003`.
- Closed ~77-block lag (4323 -> 4400) for JOIN / B-50 pin UX.

## Follow-ups

- **B-78** (suggested): teach `assert-vps-roll-ready` / `vps-roll-mfnd` to accept docs-equivalent GREEN ancestor so ALLOW_RED_CI is not needed for evidence-only pushes.
- **B-42** after B-15 PASS; lane 4 **B-76** dual-op slash continues in parallel.