# B-80 - Path A near-tip checkpoint tip-4496 (2026-07-20)

## Why

After B-79 tip-4443, JOIN waves 30-31 saw F45 lag grow again (live tip ~4496). Exact-tip Path A attestation unblocks hard light-scan pins.

## Actions

- VPS `git pull` to `d248ba2` (includes B-75 + B-79).
- `bootstrap-path-a-checkpoint-signer.sh --apply` (B-79 RPC fix) -> tip **4496** (entries=16).
- No faucet/mfnd restart; B-15 JOIN window respected.

## Proof

```
new_ckpt 4496
live_tip 4496
checkpoint_log_verify_ok entries=16
```

## CI note

**CI `#29753244727` GREEN** on B-75 `9d8bd30` (contains B-79). B-79 head CI cancelled by docs thrash.
