# B-79 - B-32 arm-ready inventory probe (2026-07-20)

## Goal

Read-only gate for live **B-32** multi-op arming: tip healthy, peers-clean, B-71 binary marker, B-78 CI roll gate, recent `last_proven` uploads, and **>=2 distinct operator hosts** (`MFN_B32_OPERATOR_HOSTS`).

Never restarts faucet/mfnd; never runs JOIN rehearsal.

## Tooling

| Path | Role |
| --- | --- |
| `assert-b32-arm-ready.sh` | `--plan-only` / `--apply` inventory |
| `b32-arm-ready-rehearsal-smoke.sh` / `.ps1` | ci-check plan needles |
| Path A tip-**4443** | near-tip checkpoint (entries=15) |
| `bootstrap-path-a-checkpoint-signer.sh` | fix: do not treat `--apply` as RPC |

## Bug fixes

1. `${2:-{}}` in `rpc_call` parsed as default `{` + stray `}`, so `list_recent_uploads` params became `{"limit":8}}` -> JSON Extra data. Fixed to `${2:-"{}"}`.
2. `bootstrap-path-a-checkpoint-signer.sh` set `RPC="${1:-}"` before getopt, so `--apply` became the RPC addr (`invalid rpc addr: --apply`). Cleared until `--rpc` / default.

## Proof (Hetzner `5.161.201.73`)

Single public host (honest NOT READY):

```
assert-b32-arm-ready: tip_height=4417
assert-b32-arm-ready: peers-clean OK
assert-b32-arm-ready: B-71 persistable-peer marker OK
assert-b32-arm-ready: mfn_ci_roll_gate: OK #29739903305 62a9c02 docs-equivalent
assert-b32-arm-ready: uploads_total=19 recent_proven=8
assert-b32-arm-ready: distinct_hosts=1 hosts=5.161.201.73
assert-b32-arm-ready: NOT READY - need >=2 distinct operator hosts
apply_exit=1
```

Synthetic dual-host READY check (`MFN_B32_OPERATOR_HOSTS=5.161.201.73,203.0.113.10`):

```
assert-b32-arm-ready: READY tip=4417 distinct_hosts=2
dual_host_exit=0
```

Path A checkpoint after bootstrap fix:

```
new_ckpt_tip 4443
checkpoint_log_verify_ok entries=15
```

## Next

Lane 4+7: register a real second distinct-host operator, then run live B-32 pack after B-15 JOIN window closes.
