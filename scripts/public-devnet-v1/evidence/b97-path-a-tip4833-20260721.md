# B-97 — Path A tip-4833 land + Windows land twin (2026-07-21)

## Why

Lane-3 B-15 wave58 reported **F45 lag=130** (ckpt frozen at **4679** while tip ~4823). That stalls hard light-scan / JOIN SUMMARY quality. VPS B-85 timer had already republished to **4833** (entries=32); the repo log was stale.

## What landed

| Field | Value |
| --- | --- |
| Unit | **B-97** (lane 7) |
| Action | `land-path-a-checkpoint-from-vps` (manual scp + new `.ps1` twin) |
| Prior repo ckpt | tip **4679** / entries=26 (B-92) |
| Landed ckpt | tip **4833** / entries=**32** |
| tip_block_id | `671e2cd04cdf710d94876a226e675079394834dea0eeab22346c3d74c32c8c05` |
| checkpoint_digest | `dfe469761072e160e6140bde935967af6f8062678134bed8bf2d9e8a931cb8de` |
| signer_id | `permawrite-maintainer-path-a-2` |
| Live tip at verify | **4825** (local observer proxy `:18734`) |
| Lag after land | **8** (threshold 16) → health OK |
| Anchor peers | `5.161.201.73:19001` / `:19002` / `:19003` |
| Genesis | `454fa5d4…a005` (public_devnet_v1) |

## Live verifies (B-15-safe)

- `assert-vps-peers-clean: OK` (no ephemeral/0.0.0.0 pollution)
- `assert-public-testnet-health --apply: OK`
  - Path A timer active; last_result=success
  - proxy tip-align ok (`hub_tip_rpc=127.0.0.1:18731`, tip_align_ms=45000, waits=474, timeouts=0)
  - faucet idle (`busy=False`, `pending_jobs=0`)
  - `tip=4825 ckpt_max=4817 lag=8 threshold=16`
- Never restarted `faucet-http` / `mfnd` / observer proxy

## Tooling delta

Added `scripts/public-devnet-v1/land-path-a-checkpoint-from-vps.ps1` (Windows apply twin). Prior rehearsal smoke assumed Git bash; Windows agents without bash could not land VPS-ahead logs (this session hit that gap).

## Findings

1. **Repo lag vs VPS log is the F45 driver, not missing publishes.** Timer fired at 02:21Z to tip-4833; local `main` still carried B-92 tip-4679 until this land. JOIN waves 49–58 paid a 100+ block hard-scan tax unnecessarily.
2. **Timer is healthy** (`path-a-near-tip-ckpt.timer` active; next ~02:51Z). Threshold=16 still correct; post-land lag=8 is green.
3. **Public `:18734` timed out from this host; local tunnel/proxy on `127.0.0.1:18734` worked.** Prefer tunnel or VPS-local health assert for outside-in probes.
4. **Faucet was idle** during land — good window; no conflict with B-15 fund path.
5. **B-96 id collision avoided:** lane-1 soak pin-assert WIP already uses B-96 locally; this unit is **B-97**.

## B-15 handoff

Lane 3 can re-pin / soft light-scan at ckpt_max=**4833** (was 4679). Expected F45 lag drop from ~130 → single digits if tip stays near 4825+.

## Remaining

- Human/assert JOIN SUMMARY still lane-3
- Real 2nd host still blocks live **B-32** (B-79 NOT READY)
- Do not cancel in-flight B-95 CI when landing this unit