# B-249 - Soft-repin readiness + Path A tip-16309 (lane3 F45)

**Unit:** B-249 (helps lane3 re-pin; elevates B-243/B-59/B-50)
**When:** 2026-08-06T13:46:00Z (approx)
**B-15-safe:** no JOIN; no faucet/mfnd restart
**Do not steal:** lane4 **B-246**; lane6 **B-13a-512**

## 1) Soft-repin prove (VPS observer RPC)

Cold wallet hung on bare light-scan at tip~16k (expected). Correct path:

```bash
export MFN_HEAVY_RPC_TIMEOUT_MS=300000
mfn-cli --wallet /tmp/.../alice.json wallet new
bash scripts/public-devnet-v1/bootstrap-wallet-from-checkpoint-log.sh --apply \
  --wallet ... --rpc 127.0.0.1:18734 --log mfn-node/testdata/public_devnet_v1.checkpoints.jsonl
bash scripts/public-devnet-v1/light-scan-checkpoint-soft.sh --rpc 127.0.0.1:18734 --wallet ... --log ...
```

| Step | Result |
| --- | --- |
| checkpoint-log verify | entries=49 max_tip=16293 (pre-republish) |
| bootstrap pin | snapshot_ok; pinned scan_height=16293; scanned to tip~16306 |
| F45 in bootstrap soft path | **PASS f45-soft** (log_max=16293 tip raced ahead) |
| light-scan-checkpoint-soft | **PASS f45-soft** scan_height=16308 |

## 2) Path A republish (lag hit threshold)

Health asserted tip=16309 ckpt_max=16293 **lag=16** FAIL → published:

```bash
bash scripts/public-devnet-v1/publish-near-tip-checkpoint-if-lag.sh --apply
```

- tip=16309 entries=50 signer=permawrite-maintainer-path-a-2 verify_ok
- land-from-vps OK → repo jsonl tip=16309
- health re-check: tip=16309 ckpt_max=16309 **lag=0** OK
- block-log: PASS tip=16308

## Lane3 note

Soft re-pin at Path A tip-**16309** is ready (was 16293). Prefer bootstrap-from-checkpoint-log before cold light-scan at tall tip. Exact-tip F12 still needs tip==log entry (soft-pass != exact-tip honesty).
