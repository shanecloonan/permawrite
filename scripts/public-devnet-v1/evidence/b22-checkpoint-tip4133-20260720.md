# B-22 — Near-tip Path A checkpoint refresh tip-4133 (2026-07-20)

## Why

B-15 wave10 reported checkpoint lag **74** (live tip ~4131 vs log max 4057). Tall-tip JOIN / B-50 bootstrap needs a near-tip signed entry so `get_light_snapshot(log_max)` is not dozens of blocks behind.

## Actions

1. Hetzner: sourced offline `/root/.mfn/checkpoint-signer.env` (never committed).
2. `publish-checkpoint-log.sh --apply` as `permawrite-maintainer-path-a-2` at tip **4133**.
3. Manifest `seed_nodes` merged into `anchor_peers`.
4. Faucet left running (`busy=true` / pending job) — not restarted.

## Verify

```
mfn-cli checkpoint-log verify mfn-node/testdata/public_devnet_v1.checkpoints.jsonl
# max_tip_height=4133 entries=6
```

## Lane notes

- Lane 3: re-pin with `bootstrap-wallet-from-checkpoint-log.sh --apply` against updated JSONL.
- Lane 7: mfnd roll still waits CI GREEN + B-51 land (do not thrash hub during B-15).
