# tip-22495 stall — CLOSE-WAIT inbound leak + 19001 restore (2026-08-15)

**Lane:** 7 / Seat C
**Host:** 5.161.201.73
**never=** faucet-http / JOIN rehearsal / voters / observer / 0.0.0.0 mfnd

## Before / after (skeptic)

| Surface | Before this unit | After |
| --- | --- | --- |
| Public `get_tip` | 22495 (stuck since hub OOM 07:30Z) | **still 22495** (`7a2c807c…`) — produce not restored |
| `mfn-p2p-forward-hub` / outside TCP `:19001` | stopped during diagnosis | **active** / **OPEN** |
| Hub `:19101` inbound | 48 CLOSE-WAIT; `inbound_cap_reached cap=48` | hub-only restart 08:11:49Z cleared to ~3; **refilled 48 CLOSE-WAIT + 9 FIN-WAIT-2** by 08:32Z (loopback `127.0.0.1`, 19001 still down for most of the refill) |
| `producer_sealed` after restart | last seal 07:26:08Z height 22495 (pre-OOM) | **none** in 40 min after 08:11 restart |
| last_proven | join-a `ea7cba7c` @ **22492** | unchanged (upload total 96) |
| Observer mempool | refill txs present | `mempool_len=2` (not mined) |

Outsider verify:

- `Test-NetConnection 5.161.201.73 -Port 19001` succeeds
- `POST http://5.161.201.73:8787/rpc` `get_tip` → `tip_height=22495`
- `list_recent_uploads` limit=2 → newest `last_proven_height=22492` commitment `ea7cba7c…`

## Apply

1. `systemctl stop mfn-p2p-forward-hub` (19001 down) — leak continued from **local** CLOSE-WAIT, so public hairpins are not the only source.
2. **Hub-only** `systemctl restart mfnd-hub` at 08:11:49Z (voters/observer/faucet left running). Replay ~12 min; RPC rebound ~08:23. NRestarts reset to 0.
3. Slots refilled to cap=48 on loopback before a seal. Vote fanout to `:19102`/`:19103` aborted EAGAIN.
4. `systemctl start mfn-p2p-forward-hub` only — never mfnd again. Outside 19001 **OPEN**.

`MFN_SKIP_MANIFEST_SEEDS=1` + loopback `peers.json` stay (B-291). Faucet still `MFND_RPC=127.0.0.1:18733`; faucet-ops **owned=1**.

## Root cause (ops, not a Rust land)

`P2P_MAX_INBOUND_HANDLERS=48` (`mfn-net/src/serve.rs`). Inbound sessions that leave the TCP stack in CLOSE-WAIT do not free the in-flight slot. Hub then rejects new handshakes (`mfnd_p2p_inbound_cap_reached`) and cannot fan out votes, so the producer cannot seal. Hub-only restart is a minutes-long band-aid; the leak returns without a protocol close/idle-timeout fix (lane 4, §6).

## Not closed

- Tip still **22495** — do not claim produce recovered.
- HTTP F7 still blocked (owned=1; refill unmined).
- Live **B-32** still needs a second distinct public host.
- Concurrent JOIN x2 still open (lock: no parallel rehearsal).

## Next

Wait for lane 4 inbound-slot close; do not thrash hub again this session. Then faucet HTTP F7; 2nd host B-32.