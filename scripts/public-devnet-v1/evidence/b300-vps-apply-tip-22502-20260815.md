# B-300 VPS apply — tip 22495 to 22502 (2026-08-15)

**Lane:** 7 / Seat C
**Host:** 5.161.201.73
**never=** faucet-http / JOIN rehearsal / 0.0.0.0 mfnd / cancel CI `#31891608943`

## Before / after (skeptic)

| Surface | Before | After |
| --- | --- | --- |
| Public `get_tip` | **22495** stuck since hub OOM 07:30Z | **22502** (`POST http://5.161.201.73:8787/rpc`) |
| VPS `mfnd` HEAD | `b1ab1b1` (B-278; pre-B-300) | built at `42b0bf49` (includes B-300 inbound hello 3s) |
| Hub `:19101` | 47 CLOSE-WAIT; `inbound_cap_reached` 14:50Z | CLOSE-WAIT **1** + ESTAB 2; **cap_n=0** |
| `producer_sealed` | none for hours | 22496 through **22502** (votes=2) |
| Outside TCP `:19001` | OPEN | **OPEN** |
| last_proven | `ea7cba7c` @ **22492** | unchanged (total 96) |

Outsider verify: `POST http://5.161.201.73:8787/rpc` `get_tip` → `tip_height>=22502`. `Test-NetConnection 5.161.201.73 -Port 19001` succeeds.

## Apply

1. Stash live `public_devnet_v1.checkpoints.jsonl`; `git pull --ff-only` to `42b0bf49`.
2. `vps-prebuild-mfnd.sh --apply` (no unit restart).
3. `MFN_ROLL_ALLOW_RED_CI=1 vps-roll-mfnd.sh --apply --skip-pull --skip-build` (scripts FAIL is Seat A; rust GREEN on B-300/B-268g). Voters then hub only. Faucet untouched.
4. Tall-tip RPC wait in `vps-roll-mfnd.sh` 300s→**900s** (replay ~15 min @ tip 22k).
5. Observer systemd-restarted on its own at 15:20Z (replay; proxy 502 until `:18734` bound). No agent observer/hub restart after roll.

`MFN_SKIP_MANIFEST_SEEDS=1` + loopback peers stay. Faucet still `MFND_RPC=127.0.0.1:18733`; idle; scan_height **22479** (F7 still Next).

## Not closed

- HTTP F7 (owned/scan catch-up).
- Live **B-32** still needs a second distinct public host.
- Path A ckpt 22495 vs tip 22502 lag=7 (threshold 8) — no republish.

## Next

faucet HTTP F7; 2nd host B-32.