# B-46 / tip-4031 recovery — hub redial after voters (2026-07-20)

Lane **7** ops on Hetzner `5.161.201.73`. Faucet **not** restarted (B-15 lock).

## Root cause

1. Hub boot-dials voters before they bind RPC → `Connection refused` → **300s peer quarantine** → vote fanout abort → tip freeze at 4031.
2. Unquoted `Environment=MFN_P2P_DIAL_EXTRA=a b c` truncated dials (systemd).
3. Public `seed_nodes` hairpin via socat amplified session flapping (VPS-local empty `seed_nodes` override; **repo seeds unchanged** for joiners).
4. `Requires=mfnd-hub` tore down voters on hub restart (lane-4 assist: `vps-soften-mfnd-requires.sh` → `Wants=`).

## Fix applied (live)

- Quoted `MFN_P2P_DIAL_EXTRA` on `mfnd-hub.service`
- `start-hub.sh` honors `MFN_P2P_DIAL_EXTRA` (local voter dials first)
- Restart sequence: voters up → **restart hub only** → dial_ok 19102/03/04
- Result: `mfnd_producer_sealed height=4032 votes=2`; tip advanced **4031 → 4033+**
- EXT `:19001` OPEN; `faucet-http` active; RPC loopback-only

## B-31 posture (re-verify)

| Check | Result |
| --- | --- |
| RPC exposure | PASS — `127.0.0.1:18731–18734` only; public `:18731` refused |
| Public P2P | PASS — socat `:19001–19003` OPEN |
| Checkpoint | PASS — tip attestation max 4028 (Path A) |
| Faucet R-4 deploy | OPEN — needs **B-26** after B-15 window |
| Tip liveness | PASS after redial (was FAIL at 4031) |

## Follow-ups

- Lane 3: resume B-15 JOIN (seeds + tip live; keep faucet lock until PASS)
- Lane 2+7: **B-26** after B-15
- Do not empty `seed_nodes` in git; VPS-local override only
