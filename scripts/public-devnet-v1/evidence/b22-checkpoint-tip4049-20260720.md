# B-22 — Near-tip Path A checkpoint refresh (2026-07-20)

## Why

Published checkpoint tip was **4028** while live tip was **~4049**. Lane 3 B-15 wave7 light-scan still had `scan_height=250` / no light checkpoint; a near-tip signed entry lets `wallet light-scan --checkpoint-log` jump to tip instead of replaying thousands of blocks.

## Actions

1. On Hetzner, sourced offline `/root/.mfn/checkpoint-signer.env` (seed never committed).
2. `publish-checkpoint-log.sh --rpc 127.0.0.1:18731 --apply` as `permawrite-maintainer-path-a-2`.
3. First append: tips **4049** then **4050** (hub fanout `anchor_peers` was loopback-only).
4. Hardened publish script to merge manifest `seed_nodes` into `anchor_peers` (drop loopback when public seeds exist).
5. Re-apply for a second near-tip entry with public seed anchors.

## Verify

```
mfn-cli checkpoint-log verify mfn-node/testdata/public_devnet_v1.checkpoints.jsonl
# max_tip_height >= 4050
```

## Lane notes

- Lane 3: re-run alice `light-scan --checkpoint-log` against updated JSONL (do not steal JOIN).
- Lane 7: seed file stays offline on VPS only.
- Faucet untouched.
