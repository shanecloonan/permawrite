# Hub OOM stall recover + Path A tip-22486 (2026-08-15)

**Lane:** 7 / Seat C
**never=** mfnd restart / faucet-http restart / parallel join-testnet-rehearsal / get_light_snapshot

## Before / after (skeptic)

| Surface | Before | After |
| --- | --- | --- |
| mfnd-hub | OOM-killed 05:36:04Z (status=9); systemd restart 05:36:09; replay done 05:46:25 @ 22484 | same pid 4115612; **no second restart** |
| Live tip | stuck **22484** (proposal 22485 starved ~9 min) | **22486** sealed (votes=2); proxy tip=22486 |
| Path A repo ckpt_max | 22475 (lag=11 FAIL) | **22486** (assert OK **lag=0**, entries=89) |
| Public 19001 | OPEN, then handshake storm on hub | stop-forward to unstick seal; **start** again; TCP **OPEN** |
| last_proven newest | 22467 (601cb854) | **22467** (upload 24e62a5a in observer/voter mempool, not mined) |
| faucet | idle | idle (not restarted) |

Outsider verify: `powershell -File scripts/public-devnet-v1/assert-outside-in-tip-ckpt-lag.ps1 -Apply` OK tip=22486 ckpt_max=22486 lag=0; POST :8787/rpc list_recent_uploads newest still 601cb854 last_proven=22467; Test-NetConnection 5.161.201.73:19001 True.

## Apply

1. Do not restart mfnd/faucet. Hub auto-restarted after OOM; replay 22484 blocks ~10 min.
2. B-42 JOIN retry: observer light-scan iris 18 blocks (owned=2). Hub `wallet upload` EAGAIN (97% CPU). Observer upload Fresh tx `d3653ced…` commitment `24e62a5a…`. Voters already had the tx; hub RPC submit timed out (server later logged submit_tx ok + mempool_save tx_count=1). Block 22486 sealed without the upload.
3. Public 19001 inbound + local handshake aborts starved vote fanout. `systemctl stop mfn-p2p-forward-hub` only (hub stayed on 127.0.0.1:19101). 22485 sealed; `systemctl start mfn-p2p-forward-hub` (B-299).
4. Path A publish via observer RPC `127.0.0.1:18734` (not hub). Land jsonl; outside-in lag OK=0. Evidence `outside-in-tip-ckpt-lag-20260815T061243Z.txt`.

## Not closed

- Live JOIN / last_proven move. Iris upload still mempool-only.
- Concurrent JOIN SUMMARY PASS.
- Live B-32 still distinct_hosts=1.
- Tip CI `#31867337251` (B-268b) FAIL = public-devnet scripts / signoff-validate (Seat A). Rust tests GREEN. Not this unit.